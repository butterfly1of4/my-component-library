import React, {Component} from 'react'
import './Form.stories.js'
import './Form.css'

const Form = (props) => {
    let classList = ''
 
let handleChange =(e)=> {
 
    
}    
return (
    <form onSubmit={this.handleChange}>
        <input />
    </form>
)
}

export default Form